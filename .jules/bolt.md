## 2024-05-15 - Array Filtering and Sorting Re-renders\n**Learning:** Complex client-side array filtering and sorting in React components causes unnecessary re-renders on every state change, even unrelated ones (like typing in a search bar).\n**Action:** Always wrap expensive array operations like `filter` and `sort` in `useMemo` hooks to prevent performance degradation on large lists during re-renders.
